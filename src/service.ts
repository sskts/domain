// tslint:disable:max-classes-per-file completed-docs
/**
 * service module
 */
import { service } from '@cinerino/domain';

export import account = service.account;
export import delivery = service.delivery;
export import offer = service.offer;
export import masterSync = service.masterSync;
export import notification = service.notification;
export import order = service.order;
export import person = service.person;
// export namespace person {
//     export import creditCard = PersonCreditCardService;
// }
export import programMembership = service.programMembership;
export import report = service.report;
export import reservation = service.reservation;
export import payment = service.payment;
export import stock = service.stock;
export import task = service.task;
export import transaction = service.transaction;
export import util = service.util;
