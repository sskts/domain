/**
 * 口座ステータス
 *
 * @namespace factory/accountStatus
 */

enum AccountStatus {
    /**
     * 開いている
     */
    OPEN = 'OPEN',
    /**
     * 閉鎖している
     */
    CLOSED = 'CLOSED'
}

export default AccountStatus;
